data:
  neuro:
    frequency: 200.0
    filter:
      - 0.5
      - 99.5
    notch_filter: null
    scaler: StandardScaler
    scale_factor: null
    clamp: 15
lora:
  # Attention (q/k/v + output proj) + the two FFN linears of every block.
  target_modules:
    - to_qkv
    - to_out
    - net.1
    - net.3
