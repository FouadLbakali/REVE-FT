data:
  neuro:
    frequency: 200.0
    filter:
      - 0.1
      - 75.0
    notch_filter:
      - 50.0
    scaler: null
    scale_factor: 10000.0  # V -> 0.1 mV
    clamp: null
lora:
  # Attention (qkv / out proj) + MLP (fc1 / fc2) inside every transformer block.
  target_modules:
    - qkv
    - proj
    - mlp.0
    - mlp.2
